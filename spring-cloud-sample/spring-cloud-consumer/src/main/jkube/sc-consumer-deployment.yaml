apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-cloud-consumer
  namespace: bjrdc-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-cloud-consumer
  template:
    metadata:
      labels:
        app: spring-cloud-consumer
