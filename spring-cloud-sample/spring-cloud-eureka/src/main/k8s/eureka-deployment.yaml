apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-cloud-eureka
  namespace: bjrdc-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spring-cloud-eureka
  template:
    metadata:
      labels:
        app: spring-cloud-eureka
    spec:
      containers:
      - name: eureka-server
        image: bjrdc206.reg/bjrdc-dev/spring-cloud-eureka:0.0.1-SNAPSHOT
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8080
