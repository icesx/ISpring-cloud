server:
  port: 8085
  servlet:
    contextPath: /sc-dashboard
eureka:
  instance:
    appname: dashboard
turbine:
  aggregator:
    clusterConfig: default
  appConfig: consumer
  clusterNameExpression: '''default'''
spring:
  application:
    name: dashboard